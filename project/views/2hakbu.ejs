<!DOCTYPE html>
<meta charset="utf-8">
<html>
  <head>
    <title>SMWU 원가분석</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Terminal+Dosis" />
    
  <style>
    html, body{
      margin:0; padding: 0;
      height: 100%;
      position: relative;
    }
  </style>
  </head>

  <body>
    <% include ./header %>
    <% include ./nav %>   
    <div id="index_section">
      
      <% include ./howtodo %>
      <div id="main_upage">
        
        <p> * 아래에 <b>학부생 등록금 납입</b> 파일을 첨부하세요.</p>
        <br/>
        <form action="/upload", method="post", enctype="multipart/form-data">
        <input type="file" name="uploadFile">
        <input type="submit" name="Upload" value="첨부">
        </form>
        <br>
        <b><p> * 아래 파일을 다운 받으시려면 파일을 클릭하세요. </p></b>

      </div>
      <div id="main_dpage">
        <b><p> * 현재 등록된 학부생 등록금 납입표</p></b><br>
        <!--<% if (false){%>
          if(table exist)<br>
          &nbsp&nbsp table 읽어와서 표 형태로 보여주기<Br>
        <% }
        else { %>
        &nbsp&nbsp 등록된 정보가 없습니다<br>
          <% } %>-->
          <table width="90%">
            <%
            if(rows.length == 0) {%>
              <p>해당 데이터가 존재하지 않습니다.</p>
            <% }
            else{ %>
              <tr style="background-color: black; color: white; font-weight: bolder;">
                <th>major_class</th> <th>major_id</th> <th>student_id</th> <th>first_major</th>
              </tr>
            <% 
              for(var i=0; i<rows.length; i++){
                var item = rows[i];
            %>
                <tr>
                  <td><%=item.major_class%></td>
                  <td><%=item.major_id%></td>
                  <td><%=item.student_id%></td>
                  <td><%=item.first_major%></td>
                </tr>
            <%
              }
            }
            %>
          </table>
      </div>

    </div>  
    <% include ./footer %>
  </body>
</html>
